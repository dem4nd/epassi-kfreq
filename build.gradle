project.ext.set("groupName", 'epassi')

group = 'epassi'

repositories {
    mavenCentral()
}

subprojects {
    repositories {
        mavenCentral()
    }
}

tasks.named('wrapper') {
    gradleVersion = '8.6'
    distributionType = Wrapper.DistributionType.BIN
}

